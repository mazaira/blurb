# Current version

Active deck you are iterating on for the seed round.

| File | Purpose |
| --- | --- |
| `template.html` | Starter scaffold — copy to `index.html` when resetting |
| `index.html` | Live deck linked from the home page at `/v/current` |

Source copy lives at the project root: [`text.md`](../text.md).

```bash
cp "current version/template.html" "current version/index.html"
```
