# Assets

Drop shared files here (e.g. `logo.svg`, `logo.png`).

They are copied to `/assets/` when you run `dev` or `build`. Reference them in HTML as:

```html
<img src="/assets/logo.svg" alt="Depa" />
```
